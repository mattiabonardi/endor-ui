import { Link } from "react-router-dom";

const EnTablePage: React.FC = () => {
  return (
    <nav>
      <ul>
        <li>
          <Link to="base">Base Data table</Link>
        </li>
      </ul>
    </nav>
  );
};

export default EnTablePage;
